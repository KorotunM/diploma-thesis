{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.local/schemas/events/parse.completed.json",
  "title": "parse.completed.v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["event_name", "header", "payload"],
  "properties": {
    "event_name": { "const": "parse.completed.v1" },
    "header": { "$ref": "#/$defs/header" },
    "payload": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "crawl_run_id",
        "source_key",
        "endpoint_url",
        "raw_artifact_id",
        "parsed_document_id",
        "parser_version",
        "raw_bucket",
        "parsed_bucket",
        "extracted_fragments",
        "metadata"
      ],
      "properties": {
        "crawl_run_id": { "type": "string", "format": "uuid" },
        "source_key": { "type": "string" },
        "endpoint_url": { "type": "string" },
        "raw_artifact_id": { "type": "string", "format": "uuid" },
        "parsed_document_id": { "type": "string", "format": "uuid" },
        "parser_version": { "type": "string" },
        "raw_bucket": { "type": "string" },
        "parsed_bucket": { "type": "string" },
        "extracted_fragments": { "type": "integer" },
        "metadata": { "type": "object" }
      }
    }
  },
  "$defs": {
    "header": {
      "type": "object",
      "additionalProperties": false,
      "required": ["event_id", "producer", "occurred_at", "schema_version"],
      "properties": {
        "event_id": { "type": "string", "format": "uuid" },
        "trace_id": { "type": ["string", "null"], "format": "uuid" },
        "producer": { "type": "string" },
        "occurred_at": { "type": "string", "format": "date-time" },
        "schema_version": { "type": "integer" }
      }
    }
  }
}
